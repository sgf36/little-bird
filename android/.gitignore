gradle-wrapper.jar
/.gradle
# Gradle writes a problems report here on every configuration, which is
# a build artefact and was landing in `git status` beside real changes.
/build/
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
