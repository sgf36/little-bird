{
  "name": "wren-comp-codes-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "migrate": "npx wrangler d1 execute wren-codes --remote --file=migrations/0001-code-role.sql",
    "test": "node --test"
  }
}
